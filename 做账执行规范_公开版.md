# 做账执行规范（GitHub 公开版）

> 这是 GitHub 专用的隐私安全版本，用来同步项目规则、执行流程和技术约束。
> 这里不得写入真实智谱 API Key、GPS Authorization、Cookie、JSESSIONID、个人账号、客户隐私或批次原始数据。
> 真实 Key 只允许保存在本机配置、打包注入流程或用户手动输入位置；公开文档只保留占位符和使用规则。

---
# 钢材磅单 / 送货单 / 调拨单 → Excel 做账强制执行规范

## 0. 强制执行原则

本文件是唯一执行规范。任何执行者，包括人工执行者和 AI 执行者，拿到本文件后必须按本文档执行，不得另行改写需求、缩小范围或自行发明替代流程。

执行过程只允许提效，不得改变最终需求和验收标准：

- 最终 Excel 仍必须可做账、可核对、可追溯原图。
- 字段规则、重复图片规则、备注规则、Excel 样式和原图追溯要求不得因流程优化而降低。
- 开始处理前，必须先检查是否已有 OCR JSON、结构化 CSV / JSON 和最终 Excel，再按本文档判断是否重跑。
- 有可用 OCR JSON 时，必须基于已有结果修整，不得直接整批重跑 OCR；但“可用”必须满足当前批次所有图片都有非空 OCR 文本、没有 `error`、没有混入旧图片记录。
- 软件默认必须在 EXE 所在位置创建 `YYYY-MM-DD_保谊达做账表` 批次总目录，所有输入、输出和留档文件都放在该总目录内，不得把多个文件夹散落在桌面。
- 软件执行区必须显示当前任务进度、百分比、预计剩余时间和滚动终端日志；中间流程区下方是唯一主日志区，不再保留底部重复日志框。
- 软件使用无边框高端界面时，必须保留窗口边缘/角落拖拽缩放能力；默认窗口应按专业工作台提供足够宽高，1080p 及以上优先使用约 `1560x960`，更高屏幕优先使用约 `1560x1000`，并按实际屏幕收敛，不得为了小窗口压缩而牺牲交互入口、复核列表、右侧规则/异常面板或日志可读性。界面风格采用克制的大厂后台风：深色石墨底、低饱和蓝灰主色、柔和分隔、少量绿色/橙色/红色状态语义；不得做成强网格监控大屏、霓虹游戏 HUD、紫色渐变或装饰性背景。
- 主界面标题栏不得渲染会遮挡文字的大图 Logo / 车图。信息架构应为左侧模块导航、顶部主动作、中间批次工作台、右侧上下文面板：顶部只放高频主动作 `导入图片`、`一键出表`、`疑问复核`、`打开结果`；左侧必须可见提供 `批次`、`OCR`、`规则管理`、`复核`、`出表设置`、`GPS 设置`、`检查更新` 和带齿轮图标的 `设置` 等入口，并在批次信息区展示质量概览、异常提醒和透明线稿车图；主要按钮、侧栏入口和设置面板入口必须使用集中 helper/registry 管理的同尺寸图片图标（建议 16px 或 18px、统一颜色、`compound=left`、统一 padding），不得用一组大小不一的 Unicode 符号直接拼在按钮文字前；普通分区标题应保持纯文本。中间必须展示批次总览、OCR 到 Excel 工作流程、疑问复核列表和执行日志；右侧承接 GPS、OCR 快速控制和 OCR/API 额度，其中 OCR 快速控制只保留 `OCR 引擎` 与动态 `OCR 接口/本地模式` 两层，不再显示 `工作模式`；引擎名称必须短、统一，在线厂商只显示 `智谱`、`阿里云`、`百度`、`腾讯云`，本地只显示 `本地 PaddleOCR CPU可用/GPU可用/需安装` 等状态；下方配置随上方引擎切换，选择智谱时显示 `综合识别`、`手写识别`、`磅单识别`、`表格识别`、`票据/单据识别` 等短场景项，选择阿里云/百度/腾讯云时显示该厂商接口短列表，选择本地 PaddleOCR 时显示 `最强/均衡/极速` 档位；OCR 引擎和接口下拉弹层必须按屏幕可用高度自适应，接口较多时默认展开更多行，但不得超出当前屏幕可见范围。OCR/API 额度只显示 compact 短行，厂商列和额度列必须对齐，不得出现手动刷新按钮或长说明。
- 软件在 Windows 通知区域 / 系统托盘显示图标时，左键单击和左键双击都必须显示/恢复主窗口，不得退出程序；Windows 任务栏按钮或右下角图标造成的外部最小化/隐藏必须自动恢复窗口，只有软件标题栏 `-` 按钮才允许主动最小化；右键必须提供常用操作菜单，至少包含显示主窗口、隐藏到托盘、一键出表、暂停/继续任务、打开待处理图片、打开输出结果、OCR 设置、检查 OCR 环境、疑问复核、规则管理、检查更新和退出；标题栏右上角第一个按钮必须使用三条杠/汉堡菜单图标并打开 `show_titlebar_menu()`，左侧底部 `设置` 按钮才使用齿轮图标并打开独立设置面板，面板内放 OCR 设置、GPS 设置、出表/Excel 设置、规则管理、检查更新、输入/输出目录和帮助等真实入口；退出只能通过右键菜单的“退出”确认或主窗口关闭流程触发，任务运行中不得让用户重复启动会冲突的任务。
- 每次重新打包 `做账执行工具.exe`，必须自动在 `项目资料\版本备份` 留下可回退版本，至少包含根目录 EXE、`_internal` onedir 运行库目录、当前源码、执行规范、使用说明和 SHA256 校验信息；版本备份不得包含明文 API Key。
- 本地 5090 主要用于提升 OCR 批处理速度、TensorRT / FP16 推理和并发吞吐，不允许让模型猜测做账字段。
- 疑问行必须单独复核；整批替换 OCR 引擎前必须先做抽样对比。

### 0.1 本机工作副本、公开同步与变更记录规则

- 原项目 `C:\Users\Administrator\Desktop\zhipu` 只读，只能作为历史参考或对照来源；不得在原项目目录里写源码、重打包、改 EXE、改 `_internal`、改批次数据或写入密钥。
- 当前实际工作副本是 `C:\Users\Administrator\Desktop\token\zhipu_workcopy`。所有复刻、验证、发布前检查和公开文档同步都必须先定位到该目录。
- GitHub 同步只允许公开安全文档白名单：`README.md`、`做账执行规范_公开版.md`、`项目复刻公开版.md`、`项目源码索引_公开版.md`、`版本历史.md`、`项目资料\说明与发布\使用说明.md`。
- 公开版不得写入真实 API Key、Cookie、GPS Authorization、JSESSIONID、账号密码、客户原图、私有 OCR JSON、私有 Excel 或批次输出；只能写环境变量名、占位文件名、安全配置入口和验证方法。
- 每次功能变更都必须同步更新相关公开文档和本机完整复刻手册；至少记录变更内容、影响范围、验证命令、真实通过/未验证状态，以及是否需要 GitHub 白名单同步。
- 提交前必须运行公开文档敏感信息扫描，并确认暂存区没有 EXE、`_internal`、私有规范、Key 文件、客户批次或输出结果。
- 本轮协作规则从“只读审查”升级为多 worker 真实执行：T0 为主控台，只做边界控制、证据汇总、冲突合并和最终审计；T2/T3/T4/T8 等模块 worker 按授权边界执行。T4 只负责公开/本机文档同步、变更日志、安全扫描证据和 GitHub 白名单建议，不修改 Python、PowerShell、Inno、EXE、`_internal`、密钥或批次数据。
- 后续 T2/T3/T4/T8 或其他模块一旦改变安装、扫描监听、文档安全、OCR/API、解析/Excel、UI 或发布行为，必须同步更新本公开规范、`README.md`、`项目复刻公开版.md`、`项目源码索引_公开版.md`、`版本历史.md` 和 `项目资料\说明与发布\使用说明.md`；本机存在完整手册时也必须同步。

## 1. 任务目标

将一个批次文件夹中的钢材磅单、送货单、发货单、调拨单照片整理为可直接做账、可直接核对、可直接交付的 Excel。

固定交付文件：

`运输录入_<日期范围>.xlsx`

固定保留页签：

- `运输录入`
- `重复图片`

固定删除页签：

- 空的 `未匹配驾驶员`
- 已经完整回填到主表 `备注` 的 `识别疑问`

---

## 2. 执行原则

- 每次执行按当前文件夹视为一个独立批次，不与历史批次合并。
- 同一批次 OCR 只跑一轮，后续基于中间 JSON、Excel 和本地原图离线修整。
- 涉及结算字段，无法确认时留空并备注，不允许猜测补全。
- 能进入主表的问题必须进入主表 `备注`，不只放在辅助页。
- 重复图片宁可少删，不可误删。
- 原图列默认使用外部原图链接，不允许用低清二次压缩缩略图替代。
- 最终 Excel 必须满足固定列结构、固定样式、可筛选、可核对、可追溯原图。

---

## 3. 输入范围

### 3.1 原始输入

- 钢材磅单照片
- 送货单照片
- 发货单照片
- 调拨单照片

### 3.2 可用辅助输入

- 驾驶员 / 车号固定对照表
- 已生成的 OCR 原始 JSON
- 已生成的结构化中间 JSON
- 已生成的重复图片清单
- 已生成的识别疑问清单

### 3.3 微信文件传输助手图片导入

如果图片来自微信文件传输助手，必须优先用微信界面直接保存图片，不得把时间浪费在读取或解密微信缓存文件上。

软件默认目录结构：

```text
YYYY-MM-DD_保谊达做账表\
  待处理图片\
  输出结果\
```

用户只需要把本批次图片放进当天批次总目录下的 `待处理图片`，最终 Excel、OCR JSON、结构化 CSV / JSON、错误日志和外部 `_原图` 文件夹都必须进入 `输出结果`。

标准做法：

- 找到文件传输助手中本批次最新图片。
- 单张图片：点开或右键图片，选择 `另存为...`，保存到 `待处理图片`。
- 多张图片：使用微信 `多选 -> 保存`，一次性保存到 `待处理图片`。
- 保存后只做文件数量、图片哈希和重复文件检查。
- 不读取 `xwechat_files` 缓存图片作为默认流程；微信缓存存在封装或加密，除非用户明确要求研究缓存，否则不得走解密路线。

### 3.4 扫描仪扫描导入

安装版软件必须在安装目录旁创建 `扫描导入` 根目录，用于承接扫描仪直接落图。

支持两种批次结构：

```text
扫描导入\2026-06-24\图片
扫描导入\2026-06-24\待处理图片\图片
```

执行规则：

- 软件启动后自动补扫 `扫描导入` 下未处理批次，运行中定时监听新批次。
- 发现图片后必须等待文件稳定，避免扫描仪仍在写入时提前处理。
- 弹窗必须显示图片数量和批次路径，按钮为 `立即一键出表`、`稍后提醒`、`忽略本批`。
- 点击 `立即一键出表` 只能设置当前图片目录、输出目录和 OCR JSON 路径，然后调用现有 `run_all()`。
- 扫描监听不得绕过 OCR 预检、空 OCR 硬停止、疑问复核、路线规则和 Excel 出表规则。
- 已处理、稍后提醒、忽略状态写入本机配置，避免同一批反复弹窗。

### 3.5 安装包和卸载

公司内部分享优先使用 Inno Setup 安装包。安装包源必须来自稳定 onedir：根目录 `做账执行工具.exe` 加同级 `_internal`。

安装规则：

- 默认 per-user 安装到 `%LocalAppData%\Programs\BoyidaAccountingTool`。
- 创建开始菜单入口，可选桌面快捷方式。
- 安装后创建 `扫描导入` 文件夹。
- 随安装复制公开安全说明文档，不复制私有规范、密钥或客户批次数据。

卸载规则：

- 标准卸载清理程序文件、快捷方式和 Inno 标准卸载注册表。
- 完整清理模式删除 `%APPDATA%\ZhipuAccountingTool`、安装目录旁 `扫描导入` 和程序生成的运行数据。
- 用户选择保留业务文件时，只保留用户明确选择的数据；完整清理模式必须尽量不留程序残留。
- 卸载脚本删除前必须校验路径在安装目录或明确 AppData 目录下，不允许越界删除。

---

## 4. 主表结构

主表页签固定命名为：

`运输录入`

主表固定 9 列：

| 列 | 列名 | 内容 |
|----|------|------|
| A | 日期 | 单据日期 |
| B | 车号 | 单据上的车牌号 |
| C | 驾驶员 | 根据车号匹配 |
| D | 重量 | 净重 / 结算重量，吨 |
| E | 货物名称 | 品名 / 材质 |
| F | 装货地 | 按货物名称规则填写 |
| G | 收货地 | 按货物名称规则填写 |
| H | 备注 | 疑问、修正、处理说明 |
| I | 原图 | 对应原图 |

列名固定：

- 使用 `驾驶员`，不使用 `司机`
- 使用 `车号`，不使用 `车牌`
- 使用 `收货地`，不使用 `卸货地`

---

## 5. 固定参考数据：驾驶员 / 车号

| 驾驶员 | 车号 |
|--------|------|
| 应云超 | 沪A19615A |
| 王从新 | 沪A25282A |
| 颜丙龙 | 沪A26202A |
| 陈实 | 浙A05633D |
| 张华勇 | 浙A06033D |
| 孙安足 | 浙A07336D |
| 郑小洪 | 浙A08165D |
| 文兴俊 | 浙A30985D |
| 乔金飞 | 浙A31318D |
| 小老表 | 浙A32233D |
| 范刚 | 浙A32236D |
| 冀明 | 浙A33875D |
| 侯泽亮 | 浙A35228D |
| 孙时奎 | 浙A36185D |
| 何大山 | 浙A82J38 |

匹配规则：

- 车号能在固定表中精确匹配时，自动填写驾驶员。
- OCR 车号存在轻微误差，且能唯一可靠修正到固定车号时，可修正为标准车号。
- 车号无法唯一确认时，车号或驾驶员留空，并在备注写明原因。
- 车号不在固定表中时，驾驶员留空，备注写 `未匹配到驾驶员`。

---

## 6. 字段规则

### 6.1 日期

日期格式固定：

`YYYY/M/D`

示例：

- `2026/5/1`
- `2026/5/11`

可用日期依据：

- 单据上明确日期。
- 毛重时间或皮重时间中的日期。
- 磅单编号中带日期时，可还原日期。

编号日期示例：

- `S260507020` → `2026/5/7`
- `B202605080086` → `2026/5/8`

处理规则：

- 日期来源一致时直接填写。
- 编号日期、毛重时间、皮重时间存在冲突时，填写可确认日期，并备注 `日期冲突需核对`。
- 不凭文件创建时间猜测单据日期，除非用户明确要求。

### 6.2 车号

- 以单据识别结果为基础。
- OCR 与固定车号表只有轻微误差，且能唯一可靠匹配时，可修正为标准车号。
- 无法确认时留空，并备注 `缺少车号` 或 `车号模糊`。

### 6.3 驾驶员

- 根据 `车号` 在固定对照表中匹配。
- 匹配不到则留空，并备注 `未匹配到驾驶员`。

### 6.4 重量

- 录入过磅单上的 `净重` 或 `结算重量`。这两个字段按同一做账重量口径处理，正常应一致。
- 原始单位为 kg 时换算为吨。
- 保留两位小数。
- 单元格中只写数字，不带单位文字。

有效读取来源：

- `净重`
- `结算重量`

处理规则：

- `净重 52010 kg` 或 `结算重量 52010 kg` 都录入为 `52.01`。
- `毛重`、`皮重`、`空重` 可以被 OCR 读取并保留在原始识别结果中，但不能填入最终重量，也不能相减推导最终重量。
- 不把车号、编号、日期、时间中的数字当作重量。
- OCR 将字段拆成多行时，结合 `净重`、`结算重量`、`kg` 等上下文判断。
- `净重` 和 `结算重量` 同时存在且冲突时，填写可确认的重量，并备注 `重量冲突需核对`；无法确认时重量留空。
- 不再按固定阈值自动备注 `重量异常,需核对`。
- 只有重量本身识别不清、位置冲突或多处重量无法唯一判断时，才备注 `重量模糊` 或 `重量冲突需核对`。

### 6.5 货物名称

归一规则：

- `合金123` → `钢坯`
- `合金XXX` / `合金xxx` → `钢坯`
- `合金坯123`、`合合坯123` 等能确认属于合金类单据时 → `钢坯`
- `卷子板` 保持 `卷子板`
- `卷予板`、`卷子领`、`卷子饭`、`签予领` 等能结合原图确认属于卷子板时 → `卷子板`
- `钠坯`、`销坯`、`销址` 等能结合原图确认属于钢坯时 → `钢坯`
- `钠渣` 等能结合原图确认属于钢渣时 → `钢渣`
- `钢渣`、`废钢`、`料头` 等其他货物按原文填写

### 6.6 装货地 / 收货地

当货物名称为 `钢渣`：

- `装货地 = 保利`
- `收货地 = 新登江丰`

当货物名称为 `卷子板`：

- `装货地 = 友谊`
- `收货地 = 保利`

当货物名称为 `钢坯`：

优先按单据顶部标题和整体打印版式判断收货地：

- `独立过磅单标题版式`：顶部以独立的 `过磅单` 四字作为标题，`浙江钧延贸易有限公司` 与标题分开显示，且整体打印版式与新登江丰样例一致时，`装货地 = 友谊`，`收货地 = 新登江丰`
- `公司名+过磅单标题版式`：顶部为 `浙江钧延贸易有限公司过磅单` 连在一起的标题，且整体打印版式与保利样例一致时，`装货地 = 友谊`，`收货地 = 保利`
- 若标题被遮挡、拍摄角度过斜、OCR 未识别出完整标题，或整体打印风格无法可靠判断，收货地留空或填 `暂定`，备注写 `钢坯收货地待核`
- 手写字只作为辅助参考，正式判断以顶部标题文字和整体打印版式为主
- 判断优先级：先按单据标题 / 版式特征判断，再套用批次特殊规则，最后使用普通默认规则
- 批次特殊规则：`5.25新登江丰` 批次为 `装货地 = 友谊`，`收货地 = 新登江丰`
- 普通默认规则：默认及 `5.25` 批次为 `装货地 = 友谊`，`收货地 = 保利`

其他货物：

- 若未命中正式内置路线规则，但原图 / OCR 明确写出路线，可按原图暂填 `装货地`、`收货地`，备注必须写 `路线按原图暂定，未命中规则，待复核` 或等价清楚说明。
- 明确路线只接受 `A到B`、`A至B`、`A->B`、`A-B` 等两端为中文地名 / 公司名的表达，或明确字段对：`装货地 / 发货地 / 起点 / 来源` 与 `收货地 / 卸货地 / 目的地 / 终点 / 送达`。
- 不得从货物名称、车号、日期、重量、单位、单价、金额、备注等噪声字段里猜地名；模型推测不能当作规则命中。
- 若原图 / OCR 也看不出明确装货地和收货地，才填写 `装货地 = 暂定`、`收货地 = 暂定`，备注写 `原图未见明确装货地/收货地，路线待核` 或等价说明。
- 按原图暂填的路线和 `暂定 / 暂定` 路线都必须进入待复核 / 路线待核统计。

程序内 `规则管理` 必须明文显示内置做账路线规则。路线规则的匹配条件必须用用户能看懂的方式写清楚，优先说明 `拉的货/货物名称`，再说明是否还需要单据标题、版式、批次说明、微信群文字或其他辅助证据；例如 `钢渣` 应显示为按 `拉的货/货物名称：钢渣` 匹配，并说明为什么对应固定路线。`一键出表` 执行顺序为：先按内置做账规则回填，再应用用户在 `规则管理` 中新增的人工纠错/路线覆盖规则；人工纠错与内置规则冲突时，以人工纠错为准，并在主表 `备注` 留痕。内置规则不得只藏在代码里不可见。

规则管理台还必须显示规则来源、内置规则数量、人工启用规则数量、停用/删除保留历史数量，以及选中规则的匹配依据、生效位置、冲突处理方式和“为什么这样匹配”。批次记录必须保存 `_applied_rules` 或等价字段，复核台能够看到每行命中的内置规则和人工覆盖规则；每条路线规则留痕至少要包含规则 id、规则说明、匹配依据 `match_basis`、原因 `reason` 和回填结果，不能只记录一个不可读的规则编号。

### 6.7 备注

备注只写真实需要核对的内容。

| 情景 | 备注 |
|------|------|
| 车号不清楚 | `缺少车号` / `车号模糊` |
| 日期不清楚 | `日期模糊` |
| 日期依据冲突 | `日期冲突需核对` |
| 重量不清楚 | `重量模糊` |
| 重量位置冲突 | `重量冲突需核对` |
| 品名待确认 | `品名待核` |
| 钢坯收货地无法判断 | `钢坯收货地待核` |
| 驾驶员未匹配 | `未匹配到驾驶员` |
| 一图多单 | `一图多单` |
| 多图一单 | `多图合并` |
| 重复图片已剔除 | `重复N张已合并` |
| 原图缺失 | `原图缺失` |

禁止事项：

- 不使用自动阈值类备注 `重量异常,需核对`。
- 不把关键问题只放在辅助页。
- 不为了凑完整而猜测车号、重量、日期或货物名称。

---

## 7. 重复图片判定规则

### 7.1 总原则

重复剔除必须谨慎。

宁可保留重复，也不能把不同单据误删。

### 7.2 可以判定重复的情况

满足以下任一条件，才可以从主表剔除重复项，并写入 `重复图片` 页：

1. 原图文件哈希完全一致。
2. OCR 和原图核对后确认是同一张单据，且同时满足：
   - 单据编号一致
   - 完整毛重时间 / 皮重时间一致，或至少一个完整关键时间一致
   - 车号一致
   - 结算重量一致
   - 货物名称一致

### 7.3 不得判定重复的情况

以下情况不得作为重复剔除依据：

- 只有日期、车号、重量、货物名称相同。
- 只有重量相同。
- 单据编号不同。
- 完整毛重时间或皮重时间不同。
- 序号不同但其他结算字段巧合一致。
- OCR 缺少单号或缺少完整时间，无法证明是同一张单。

示例：

- `B202605050100` 和 `B202605050043` 即使车号、重量、货物相同，也不是重复，因为序号和完整时间不同。

### 7.4 重复图片页格式

页签名固定：

`重复图片`

固定两列：

| 重复图片 | 对应原图 |
|----------|----------|

主表保留 `对应原图` 那一条。

如果保留行需要承载重复说明，主表备注写：

`重复1张已合并`

---

## 8. 原图列要求

### 8.1 核心要求

`原图` 列默认写入外部原图链接，原始图片文件放在最终 Excel 旁边的同名 `_原图` 文件夹中。

必须做到：

- 一条主表记录对应一张原图。
- `原图` 列显示对应图片文件名，并写入可点击相对链接。
- 图片文件名必须包含序号、日期、车号、驾驶员和原始文件名等可读信息。
- 筛选、排序、移动行时，`原图` 链接单元格必须跟随对应行。
- 必须生成 `原图索引.csv`，记录生成时 Excel 行号、链接文件名、原图相对路径、原始文件名、日期、车号、驾驶员和重量。
- 交付文件中的原图链接必须是相对路径，不得依赖本机用户目录下的绝对路径。
- 打开或查看图片时保持原图清晰度。

### 8.2 禁止做法

禁止为了缩小文件体积，把原图替换成二次压缩的小图。

不允许：

- 先生成低清缩略图再嵌入 Excel。
- 把原图压成模糊马赛克效果。
- 只写普通文件名但没有可点击链接和外部原图文件。
- Excel 与 `_原图` 文件夹脱离，导致交付后无法打开原图。

### 8.3 推荐实现方式

- 将原始 JPG/PNG 复制到 `运输录入_<日期范围>_原图` 文件夹。
- Excel `原图` 列写入相对链接，不把大图塞进工作簿。
- `原图` 列宽度可参考 `28`。
- 数据行高度可参考 `24`。
- 如确实需要单文件交付，可在软件中选择“格内嵌图（文件大）”作为可选模式。

### 8.4 筛选、排序和文件名关系

外部原图文件名在生成 Excel 时一次性确定。用户在 Excel 里筛选、排序或移动整行时，不要求、也不应该自动重命名外部图片文件。

必须保证：

- `原图` 单元格作为该行数据的一部分一起移动。
- 单元格里的相对链接始终指向该行对应的外部原图文件。
- `_原图` 文件夹和 Excel 一起复制、压缩或发送后，链接仍能打开。

如果用户在 Excel 生成后手工修改日期、车号、驾驶员等字段，外部图片文件名不会自动跟着改。需要文件名也按修改后的字段更新时，必须回到工具重新生成 Excel 和 `_原图` 文件夹。

说明：

- 默认外部链接模式用于解决 Excel 文件过大、打开和滚动卡顿、同事传输困难的问题。
- 不能为了轻量化牺牲原图清晰度和行级追溯关系。

---

## 9. Excel 样式标准

### 9.1 字体

全表字体统一：

`宋体`

推荐字号：

`10`

### 9.2 对齐

全表所有单元格：

- 水平居中
- 垂直居中
- 必要时自动换行
- 所有已用表格区域必须补齐空白单元格样式，不能只给有文字的单元格加样式
- 表格内文字和空白格都必须看起来属于同一张连续表

适用范围：

- 日期
- 车号
- 驾驶员
- 重量
- 货物名称
- 装货地
- 收货地
- 备注
- 原图列
- 辅助页

### 9.3 表头

表头要求：

- 宋体
- 加粗
- 水平居中
- 垂直居中
- 可使用浅色填充

### 9.4 边框

所有最终 Excel 表格默认要求：

- 主表和辅助页的已用矩形区域必须整体加黑色细边框
- 表头、数据行、备注列、空白占位格都必须有边框
- 不允许只给主体数据列加框，导致备注列或外圈空白区域看起来断开
- 对账类或会计清单类输出也必须遵守同样边框规则

### 9.5 日期、筛选和冻结

日期列要求：

- 日期列必须优先写成真实 Excel 日期单元格，不得默认写成普通文本
- 显示格式按业务要求设置；对账类、会计清单类最终表默认使用 `yyyy-mm-dd`
- 日期列作为真实日期后，Excel 筛选下拉必须能按日期层级分类

主表要求：

- 开启筛选
- 冻结首行

---

## 10. 执行流程

### 10.1 批次预检查

- 确认本次只处理一个批次文件夹，不与历史批次混合。
- 软件启动后必须自动创建当天 `YYYY-MM-DD_保谊达做账表` 批次总目录，并在其中创建 `待处理图片` 与 `输出结果`。
- 清点原图数量，排除明显无关文件、测试文件、缓存文件。
- 检查是否已有 `paddle_ocr_raw.json`、`transport_parsed_records.json`、结构化 CSV 和最终 Excel。
- 已有完整中间文件时，先做验收和疑问复核，不直接重跑整批 OCR。
- 已有 `paddle_ocr_raw.json` 只有在当前批次每张图片都有非空 OCR 文本、没有 `error`、没有缺图、没有旧图记录时，才允许跳过 OCR。
- 非单据照片中如果包含有效单据区域，可保留并进入主表，同时保留原图便于核对。

### 10.2 OCR 获取

执行顺序固定：

1. 已有原始 OCR JSON 可用时，必须直接进入结构化解析和复核。
2. 原始 OCR JSON 缺失、损坏，或用户明确要求更换 OCR 引擎时，才重新 OCR。
3. OCR 引擎由执行工具按硬件和环境检测结果推荐，不由人工凭感觉切换。
4. 普通分享场景默认使用在线 GLM-OCR，以保证对方不需要安装 Python、PaddleOCR、CUDA 或本地 OCR 环境。
5. 检测到强 NVIDIA GPU，且 PaddleOCR、PaddlePaddle GPU、CUDA、`paddle.utils.run_check()` 均通过时，才推荐切换本地 PaddleOCR。

一键出表前必须自动执行 OCR 预检，不允许把“检查 OCR 环境”做成只靠用户手动点击的摆设：

- 必须检查 `待处理图片` 中是否存在可处理图片。
- 必须检查已有 OCR JSON 是否完整可用；失败、空文本、缺图、混入旧图都必须自动重跑 OCR。
- 默认在线 GLM-OCR 时，必须检查 API Key 是否可用；缺 Key 必须弹窗提醒，停止生成 Excel。
- 选择本地 PaddleOCR 时，必须自动检查本地 Python、PaddleOCR、PaddlePaddle、CUDA / `run_check()`；环境不完整时不得强行切回在线 GLM-OCR，必须保持用户的本地选择，并进入本地 OCR 安装/配置流程。
- 本地 OCR 安装流程必须优先使用现有 Python；找不到 Python 时可通过 Windows `winget` 安装 Python 3.12；然后安装 PaddleOCR 和 PaddlePaddle。只有 NVIDIA 显卡才按 GPU 包处理，虚拟机显卡、核显、Microsoft Basic Render Driver 或未检测到 NVIDIA 时必须安装 CPU 版 PaddlePaddle。Windows 50 系显卡按 PaddleOCR 官方 50-series wheel；GPU 未通过但 CPU Paddle 可用时允许用 CPU 本地模式，并明确提示速度较慢。
- 本地 OCR 跑批时不得把用户选错档位直接变成弹窗失败；极速/HPI、极限/TensorRT 或 GPU 初始化失败时，必须自动降级到更稳的本地档位，必要时改用稳妥 CPU。只有所有本地档位都失败时才停止，并把失败原因写入进度日志和错误日志。
- OCR 有任何失败或返回空文本时，必须停止生成 Excel，不允许继续生成空白或错误表格。

本地 PaddleOCR 重跑命令（可选高级模式）：

```powershell
python scripts\run_paddle_ocr.py `
  --input-dir 5.31 `
  --output paddle_ocr_raw.json
```

执行要求：

- 整批 OCR 只跑一轮。
- 支持断点续跑时，已有 `file_name` 的图片应跳过。
- 重跑 OCR 会改变识别文本，必须重新走结构化解析、疑问复核和最终验收。

### 10.3 结构化解析

将 OCR JSON 转为主表数据、重复图片清单和疑问清单。

```powershell
python scripts\parse_transport_records.py `
  --input paddle_ocr_raw.json `
  --output-json transport_parsed_records.json `
  --output-csv 运输录入_结构化预览.csv `
  --duplicates-csv 重复图片.csv `
  --questions-csv 识别疑问_已回填主表.csv
```

结构化规则：

- 日期统一为 `YYYY/M/D`。
- 车号映射到标准车号。
- 驾驶员按固定表匹配。
- 重量统一换算为吨，保留两位小数。
- 品名按规则归一。
- 装货地、收货地按货物名称回填。
- 重复剔除必须按第 7 节规则执行，不得只用主表字段相同来剔除。

### 10.4 人工复核疑问行

复核对象：

- 日期冲突、日期模糊。
- 车号模糊、缺少车号、未匹配到驾驶员。
- 重量模糊、重量冲突。
- 品名待核。
- 钢坯收货地待核。
- 一图多单、多图合并、疑似重复。

处理规则：

- 能确认大部分字段的行必须进入主表。
- 无法确认的字段留空。
- 所有关键疑问必须写入主表 `备注`，不能只留在辅助 CSV。
- 复核时可对单张疑问图使用 GLM-OCR 或人工看原图，不反复整批 OCR。
- 复核台应支持连续处理：保存修正或确认无误后优先跳到下一条待复核记录；仍无法确认时可保存为 `待复核`；键盘操作至少支持保存修正、确认无误、打开原图、上一条/下一条和关闭窗口。

### 10.5 Excel 生成与最终验收

生成 Excel：

```powershell
python scripts\build_transport_excel.py `
  --input-json transport_parsed_records.json `
  --output 运输录入_2026.5.12-2026.5.31.xlsx
```

生成后检查：

- 主表 `原图` 链接数量是否等于主表数据行数。
- `_原图` 文件夹是否存在，文件数是否等于主表数据行数。
- 点击 `原图` 列链接后，是否能打开对应清晰原图。
- `原图索引.csv` 是否能追溯生成时 Excel 行号、链接文件名、原图相对路径和原始文件名。
- `重复图片` 有内容时保留。
- 空的 `未匹配驾驶员` 删除。
- `识别疑问` 如果问题已进入主表备注则删除。
- 最终按第 11 节和第 15 节逐项验收。

---

## 11. 最终检查清单

交付前逐项检查：

- 主表页签是否为 `运输录入`
- 是否只保留需要的页签
- 主表是否为 9 列
- 列名和列序是否正确
- 日期是否统一为 `YYYY/M/D`
- 重量是否为吨，且保留两位小数
- 是否已取消自动 `重量异常,需核对`
- `合金123`、`合金XXX` 等是否统一为 `钢坯`
- `钢渣` 是否统一为 `保利` / `新登江丰`
- `卷子板` 是否为 `友谊` / `保利`
- `钢坯` 是否优先按 `独立过磅单标题版式` 和 `公司名+过磅单标题版式` 区分收货地
- `钢坯` 是否按规则填写：独立 `过磅单` 标题版式为 `友谊` / `新登江丰`，公司名连写 `过磅单` 标题版式为 `友谊` / `保利`
- 无法判断钢坯收货地时，是否未猜测补全，并备注 `钢坯收货地待核`
- `钢坯` 批次规则是否仍正确：默认及 `5.25` 为 `友谊` / `保利`，`5.25新登江丰` 为 `友谊` / `新登江丰`
- 未命中正式路线规则时，原图 / OCR 明确路线是否已按原图暂填并备注待复核
- 原图 / OCR 也没有明确路线时，是否为 `暂定` / `暂定` 并备注路线待核
- 按原图暂填路线与 `暂定` 路线是否全部进入路线待核统计和待复核清单
- 驾驶员是否按固定车号表匹配
- 未匹配驾驶员是否备注
- 疑问是否已写入主表备注
- 重复图片是否只保留真实重复
- 是否没有因为同车同重量误删不同单据
- `重复图片` 页是否只有两列：`重复图片`、`对应原图`
- 原图列是否为可点击外部原图链接
- `_原图` 文件夹是否与 Excel 同目录交付
- 原图打开后是否清晰，不是马赛克缩略图
- 原图链接数和外部原图文件数是否等于主表数据行数
- `paddle_ocr_raw.json` 是否覆盖当前批次全部图片，且没有 `error`、空文本、缺图和旧图记录
- 本批次所有文件是否都位于 `YYYY-MM-DD_保谊达做账表` 批次总目录内
- 全表字体是否为宋体
- 全表是否水平、垂直居中
- 最终文件名是否清楚

---

## 12. 必须留档文件

除最终 Excel 外，以下文件必须留档：

- `paddle_ocr_raw.json`
- `transport_parsed_records.json`
- `重复图片.csv`
- `识别疑问_已回填主表.csv`
- `重量复核明细.csv`
- `scripts/run_paddle_ocr.py`
- `scripts/parse_transport_records.py`
- `scripts/build_transport_excel.py`
- 原始图片文件夹

软件分享给同事使用时，默认稳定版交付根目录 `做账执行工具.exe` 以及同级 `_internal` 运行库目录，建议压缩成一个 zip 后发送；同事只需要双击根目录 `做账执行工具.exe`。如果业务明确要求“只发送一个 EXE、不带任何其他文件”，必须使用 `项目资料\开发源码\设置内置Key并打包.ps1 -OneFile` 生成单文件分享版，并发送 `项目资料\单文件分享版\<构建时间>\做账执行工具.exe` 这一单个文件。首次运行后由软件自动创建当天 `YYYY-MM-DD_保谊达做账表` 批次总目录；同事只需要把图片放入其中的 `待处理图片`，不得要求同事手工在桌面创建多个散乱目录。

开发者每次打包新版 EXE 时，打包脚本必须同时写入 `项目资料\版本备份`。构建前旧版和本次新版都要能从备份目录直接找回；稳定版备份必须同时保存根目录 EXE 和 `_internal` 运行库目录，避免 onefile 首次自解包触发 `CreateProcessW: 拒绝访问` 弹窗。单文件分享版备份必须标记为 `onefile`，且发送前必须把该 EXE 单独复制到空目录，运行 `--self-test`、`--startup-smoke-test` 和正常启动检查；若任一检查失败，不得把该单文件版称为可交付。备份目录只用于本地回退，不作为普通同事使用时必须发送的文件。

---

## 13. Markdown 编码规范

执行文档统一保存为：

`UTF-8 with BOM`

原因：

- Windows 中文环境下，部分软件会把无 BOM 的 UTF-8 文件误判为 GBK。
- 误判后中文会显示为乱码。
- 使用 UTF-8 with BOM 后，常见编辑器能稳定识别中文编码。

VS Code 推荐设置：

```json
{
  "files.encoding": "utf8bom",
  "files.autoGuessEncoding": false
}
```

当前文件修复步骤：

1. 打开 Markdown 文件。
2. 点击 VS Code 右下角编码。
3. 选择 `Reopen with Encoding`。
4. 选择 `UTF-8`。
5. 确认中文显示正常。
6. 再点击右下角编码。
7. 选择 `Save with Encoding`。
8. 选择 `UTF-8 with BOM`。
---

## 14. OCR 配置和强制分层策略

### 14.1 OCR 引擎推荐规则

OCR 不固定死用某一个引擎，必须先按软件检测结果选择：

- 普通分享、无本地环境、无 NVIDIA GPU、虚拟机环境、或使用者不懂环境配置时，默认在线 GLM-OCR。
- 本机有 5090 / 高显存 NVIDIA GPU，且 PaddleOCR、PaddlePaddle GPU、CUDA、`paddle.utils.run_check()` 全部通过时，优先本地 PaddleOCR PP-OCRv5。
- 有强 GPU 但 Paddle 环境不完整时，如果用户选择在线 OCR，当前批次先用在线 GLM-OCR；如果用户明确选择本地 OCR，软件必须先尝试补齐本地环境，不得静默改用在线 OCR。
- 无 NVIDIA GPU 或在虚拟机中选择本地 OCR 时，软件必须明确提示将使用 CPU 版 PaddlePaddle；CPU 能运行但较慢，不得显示成调用独立显卡。
- 已有可用 OCR JSON 时，不因引擎推荐变化而整批重跑。
- 软件界面必须使用中文引擎名和中文档位名，不得把 `glm`、`paddle`、`stable`、`fast`、`trt` 直接暴露给普通用户；本地档位名称后必须用方括号标明适用设备，例如 `[CPU/GPU]`、`[GPU优先]`、`[仅GPU]`。

适用场景：

- 232 张左右的批量单据照片。
- 需要离线处理、断点续跑和保留原始 OCR JSON。
- 需要稳定拿到文本框、识别文本、置信度和空间位置供后续规则解析。

固定原则：

- 未完成抽样对比前，禁止为了追求“看起来更智能”而频繁切换 OCR 引擎。
- PaddleOCR 输出作为主解析链路，业务规则负责最终字段判断。
- OCR 只提供证据，不直接决定做账字段。

### 14.2 本地 5090 优化

5090 的优势主要是本地推理速度、TensorRT / FP16 和并发吞吐，不是自动提高业务判断正确率。

首次配置或重装运行环境时，必须先确认：

```powershell
python --version
python -m pip --version
nvidia-smi
```

Windows 50 系显卡必须先按 PaddleOCR 官方 50-series GPU wheel 说明判断适配方式；如果官方稳定版已支持当前驱动，再按 PaddlePaddle 官方 cu129 / cu130 安装路线执行。具体命令以官方页面最新说明为准。

安装后必须验证：

```powershell
python -c "import paddle; paddle.utils.run_check(); print(paddle.is_compiled_with_cuda())"
```

只有验证通过，才认为 GPU 环境可用于整批 OCR。

### 14.2.1 PaddleOCR 调用模板

整批 OCR 脚本必须只初始化一次 `PaddleOCR`，然后循环处理图片；禁止每张图片重新初始化 OCR 对象。

稳定档调用，作为正式处理基线：

```python
ocr = PaddleOCR(
    lang="ch",
    ocr_version="PP-OCRv5",
    device="gpu:0",
    use_doc_orientation_classify=True,
    use_doc_unwarping=True,
    use_textline_orientation=True,
    text_recognition_batch_size=8,
    textline_orientation_batch_size=8,
)
```

5090 加速档调用，小样本准确率不下降时用于整批：

```python
ocr = PaddleOCR(
    lang="ch",
    ocr_version="PP-OCRv5",
    device="gpu:0",
    enable_hpi=True,
    use_doc_orientation_classify=True,
    use_doc_unwarping=True,
    use_textline_orientation=True,
    text_recognition_batch_size=16,
    textline_orientation_batch_size=16,
)
```

TensorRT / FP16 条件档调用，只能在 TensorRT 环境验证通过后使用：

```python
ocr = PaddleOCR(
    lang="ch",
    ocr_version="PP-OCRv5",
    device="gpu:0",
    enable_hpi=True,
    use_tensorrt=True,
    precision="fp16",
    use_doc_orientation_classify=True,
    use_doc_unwarping=True,
    use_textline_orientation=True,
    text_recognition_batch_size=16,
    textline_orientation_batch_size=16,
)
```

参数调整规则：

- `enable_hpi=True`：小样本验证通过后启用高性能推理策略。
- `use_tensorrt=True`：TensorRT 环境验证通过后启用。
- `precision="fp16"`：小样本字段准确率不下降时启用。
- `batch_size`：按显存和稳定性逐步增加，不得一次性拉满。
- `text_recognition_batch_size`：优先从 `8` 提到 `16`；仍稳定且显存富余时再试 `24` 或 `32`。
- `textline_orientation_batch_size`：与识别 batch 同步调大；如显存波动或报错，先降回 `8`。
- `use_doc_unwarping=False`：只允许在样本确认图片不歪斜且字段准确率不下降时关闭；单据照片倾斜明显时必须保持开启。
- `text_rec_score_thresh`：不得随意调高，低置信度文本仍可能用于人工复核和备注判断。

以上加速项必须先小样本试跑，不得未经验证直接用于正式整批。

小样本试跑必须使用至少 20 张图片，覆盖钢坯、卷子板、钢渣、废铁类单据，以及日期冲突、车号模糊、重量模糊样例。对比通过标准：

- 主字段准确率不低于稳定档。
- 不新增漏识关键字段。
- 单张平均耗时明显下降。
- 输出结构仍能被后续解析脚本读取。

### 14.3 GLM-OCR 复核策略

GLM-OCR 仅限用于：

- 日期冲突、车号模糊、重量模糊等疑问行。
- 钢坯收货地待核、版式判断困难的单据。
- 表格、印章、复杂排版或普通 OCR 明显漏识的图片。
- OCR 引擎替换前的抽样对比。

GLM-OCR 可作为普通分享版默认整批 OCR，也可用于疑问行复核；在本地 5090 + Paddle GPU 环境完整时，是否替换为本地 PaddleOCR 必须先按 20 张样本比较字段准确率、漏识率、速度和成本。

样本对比必须记录：

- 字段准确率。
- 漏识和误识情况。
- 单张耗时和整批预计耗时。
- 本地算力或 API 成本。
- 对现有解析脚本结构的影响。

只有 GLM-OCR 在样本中明显优于 PaddleOCR，且输出能稳定转成 `paddle_ocr_raw.json` 等价结构时，才可替换整批 OCR 链路。

### 14.4 OCR 方案库

当前软件可直接调用的主方案：

- 在线智谱 GLM-OCR：普通分享默认方案，对方不需要安装本地 OCR 环境。
- 阿里云手写 OCR：已接入程序 `OCR 引擎` 选项，可用于手写备注、送货凭证和复杂票据抽样或整批对照。
- 百度手写 OCR：已接入程序 `OCR 引擎` 选项，可用于手写备注和中文票据抽样或整批对照。
- 腾讯云通用手写体 OCR：已接入程序 `OCR 引擎` 选项，可用于手写补充字、签字、手写备注抽样或整批对照。
- 本地 PaddleOCR PP-OCRv6 medium/small/tiny：本机 5090 / 高显存 GPU 环境完整后用于批量加速，默认使用准确率最高的 medium。

本轮已接入源码的独立在线引擎：

- 千问视觉 OCR（百炼 OpenAI 兼容接口）：定位为独立在线 OCR 引擎，不并入智谱、阿里云、百度、腾讯云或本地 PaddleOCR；默认 / 优先接入模型为官方 OCR 专用 `qwen-vl-ocr`，同时可选 `qwen3-vl-plus` 和兼容旧 `qwen3.7-plus`，模型名必须可配置。用户口述“千问 3.7 Plus 视觉模型”时按兼容视觉复核模型记录，做整批 OCR 默认仍使用 `qwen-vl-ocr`。源码已接入配置读取、请求适配、UI 入口和中间层输出；公开复刻只记录安全接口边界，不记录任何真实密钥或客户数据。

OCR 接口目录和中间层规则：

- `OCR 设置 -> 接口目录` 按厂商和分类展示智谱、阿里云、百度、腾讯云接口能力；本地 PaddleOCR 不混入在线 API 额度或接口目录。
- 若主源码落地千问视觉 OCR，接口目录必须单独展示 `千问视觉 OCR（百炼）`，并区分 `可直接调用`、`目录项`、`需补参数`；未完成配置读取、请求适配、错误处理和中间层输出前，不得在 UI 或文档里伪装成已跑通。
- 接口目录必须区分 `可直接调用`、`目录项`、`需补参数`。只有 `可直接调用` 允许被设为当前批量 OCR 接口。
- 右侧 OCR 快速控制的第二个下拉必须随当前引擎动态刷新：选择智谱时展示 `综合识别`、`手写识别`、`磅单识别`、`表格识别`、`票据/单据识别` 等已接入场景配置；选择阿里云、百度、腾讯云时展示该厂商 OCR 能力目录的全量短列表，并用 `可直接调用`、`目录项`、`需补参数` 状态区分；选择本地 PaddleOCR 时展示本地模式档位。非 `可直接调用` 项可以展示和打开文档/开通控制台，但不得保存为批量 OCR 当前直调接口，也不得伪装成已跑通。
- 阿里云接口目录优先使用随包 SDK 中的 `Recognize*Request` 自动发现，再叠加程序维护的常用中文名称和分类；百度和腾讯云必须维护官方能力目录，不能只登记少数几个已接入接口。
- 接口目录弹窗必须提供官方文档和开通/控制台入口。开通云服务涉及账号、费用或服务协议时，只能引导用户在厂商控制台确认，不得由软件静默代开通。
- 百度必须展示官方 `磅单识别` 接口：`https://aip.baidubce.com/rest/2.0/ocr/v1/weight_note`。结构化返回先进入 OCR 临时结果，不直接跳过解析/复核写入最终表。
- 腾讯云接口切换时必须同步 `X-TC-Action` 和 TC3 签名。
- 每次在线 OCR 写入 `paddle_ocr_raw.json` 后，应同时更新同目录的 `*_intermediate_preview.json`；`OCR 设置 -> 临时结果` 展示的是 OCR 返回数据的可读中间层，不是最终 Excel。
- 真实额度以厂商控制台/账单接口为准；识别接口响应没有余额字段时，不得把本地调用次数伪装成官方剩余额度。

腾讯云通用手写体 OCR 的本机测试流程：

```powershell
$ProjectRoot = "<项目根目录>"
Set-Location "$ProjectRoot\项目资料\开发源码"
python run_tencent_ocr_test.py --configure
python run_tencent_ocr_test.py --input "$ProjectRoot\2026-06-02_保谊达做账表\待处理图片" --limit 6
```

`--configure` 必须使用隐藏输入保存 SecretKey；不得把真实密钥写进命令行、脚本、报告或文档。测试输出必须进入该批次 `输出结果` 目录，至少包含 `tencent_handwriting_ocr_test_report_*.json` 和成功时的 `tencent_handwriting_ocr_raw_*.json`。

后续可接入或用于疑问行复核的方案：

- 通义 Qwen-OCR / Qwen-VL：优先作为在线备选和中文复杂票据复核方案。
- PaddleOCR PP-StructureV3 / PP-ChatOCRv4：用于表格、版式和关键信息抽取增强。
- 本地 Qwen3-VL-30B-A3B-Instruct / Qwen3-VL-32B-Instruct：本机 32GB 显存优先考虑的本地视觉复核模型，用于疑问行、手写备注、复杂版式和抽样对比，不默认整批全量跑。
- 本地 Qwen2.5-VL-32B / GOT-OCR2 / Surya：作为兼容备选和对照基线；Qwen2.5-VL 不是主推新方案。
- Mistral OCR：用于 PDF / 文档解析场景的在线备选。

方案库规则：

- 新方案接入前，必须先抽样至少 20 张，比较字段准确率、漏识率、速度、成本和输出结构兼容性。
- 不允许默认多引擎全量重复跑整批图片；默认流程是主引擎整批、疑问行复核、人工确认。
- 任何新模型输出都必须经过业务规则解析和疑问标记，不得直接让模型猜做账字段。
- 千问视觉 OCR 的模型输出也只能作为 OCR 文本 / 中间层输入，后续仍必须走解析、疑问复核、Excel 生成和原图追溯链路；空 OCR、API 错误、图片覆盖不完整或中间层为空时必须硬停止，不能生成空白或半可信 Excel。

### 14.5 密钥规则

- `ZHIPU_API_KEY` 只能保存在软件输入框、本机 `.env` 或系统环境变量中。
- 阿里云、百度、腾讯云 OCR Key 只能保存在本机环境变量、本机加密配置，或本机私有 `API KEY` 文件夹中；不得通过命令行参数、Markdown、JSON、CSV、Excel 明文传递。
- 千问视觉 OCR（百炼 OpenAI 兼容接口）密钥只允许来自本机环境变量、本机安全配置或本机私有导入文件；可识别的环境变量名只登记为 `DASHSCOPE_API_KEY`、`BAILIAN_API_KEY`、`QWEN_API_KEY`，公开文档不得填写任何值。环境变量优先于本机保存配置和桌面 `API KEY` 导入文件；模型名、`workspace_id`、`region` 和 endpoint/base URL 可随环境变量或本机导入文件覆盖。默认 `qwen-vl-ocr` 优先按百炼工作空间 OpenAI 兼容地址调用；若配置了 `workspace_id`，程序按 `workspace_id.region.maas.aliyuncs.com/compatible-mode/v1` 自动生成 endpoint，并保留默认 DashScope endpoint 作为兜底候选。
- `--qwen-key-template` 只允许在本机私有 `API KEY` 文件夹生成 `qwen.example.txt` 模板；用户可在本机复制为 `qwen.txt` / `bailian.txt` / `dashscope.txt`，或使用 `qwen.csv` / `bailian.csv` / `dashscope.csv`。文本文件可写 Key、`model: qwen-vl-ocr`、`workspace_id: ...`、`region: cn-beijing` 和可选 `endpoint: ...`；CSV 只登记字段名 `DASHSCOPE_API_KEY` / `BAILIAN_API_KEY` / `QWEN_API_KEY`、`model`、`workspace_id`、`region`、`endpoint` / `base_url`，不得在文档、命令行、源码或 Git 中写入字段值。
- 软件内 `OCR 设置 -> 千问配置` 可弹窗写入本机 DPAPI 配置并立即探针，`OCR 设置 -> 测试千问` 可测试已有配置；命令行 `--qwen-preflight` 可先做无联网、无泄密预检，`--qwen-configure-key` 可打开同类配置弹窗，`--qwen-import-key-from-clipboard` 可从当前剪贴板提取 Key/模型/workspace/region 字段并隐藏保存后立即探针。所有入口都不得打印 Key。
- `--qwen-live-probe` 是真实调用探针，只读取本机安全凭据；无 Key 时应返回 `QWEN_LIVE_PROBE_MISSING_KEY`，有 Key 且模型开通时才可返回 `QWEN_LIVE_PROBE_OK`，输出范围限于模型、来源、endpoint 类型和识别行数，不得打印密钥或识别全文。
- 腾讯云 `TENCENTCLOUD_SECRET_ID` / `TENCENTCLOUD_SECRET_KEY`、阿里云 `ALIYUN_ACCESS_KEY_ID` / `ALIYUN_ACCESS_KEY_SECRET`、百度 `BAIDU_OCR_API_KEY` / `BAIDU_OCR_SECRET_KEY` 都不得写进源码或公开打包产物。
- 打包可包含阿里云 SDK 的 `.vendor-sdk` 依赖目录，但不得包含任何真实 API Key。
- 不得把真实 API Key 写入 Markdown、脚本、JSON、CSV、Excel 或公开仓库。
- 公开 GitHub Release 的 EXE 默认不得内置 API Key；打包脚本必须默认生成无内置 Key 版本。
- 只有私下指定人员使用的内部包，才允许显式使用 `-EmbedKey` 生成内置 Key 版本。
- 如果 API Key 泄露、失效或更换，只更新本机环境，不在执行文档中记录明文。

### 14.6 OCR 已知处理注意事项

- OCR 返回如出现中文乱码，应先做编码还原后再解析字段。
- `卷子板` 可能被误识别为 `电子板`、`番子板` 等，需结合原图和版式复核后按规则归一。
- `钢渣` 可能被误识别为 `钾渣`、`锅渣`、`铆渣`、`钠渣` 等，需结合原图和上下文复核后处理。
- 日期、单号年份、车号中间符号可能出现误读，必须结合毛重时间、皮重时间、编号和原图核对。
- 对结算字段不得猜测补全；无法确认时留空并写入主表备注。

参考资料：

- PaddleOCR 安装与 50 系 GPU：`https://www.paddleocr.ai/main/en/version3.x/paddlepaddle_installation.html`
- PaddleOCR 性能优化 FAQ：`https://www.paddleocr.ai/main/en/FAQ.html`
- GLM-OCR 官方文档：`https://docs.bigmodel.cn/cn/guide/models/vlm/glm-ocr`
- 阿里云手写文字识别：`https://help.aliyun.com/zh/ocr/developer-reference/api-ocr-api-2021-07-07-recognizehandwriting`
- 百度手写文字识别：`https://ai.baidu.com/ai-doc/OCR/Nk3h7y2r7`
- 腾讯云通用手写体识别：`https://cloud.tencent.com/document/product/866/36212`
- 通义 Qwen-OCR API：`https://www.alibabacloud.com/help/en/model-studio/qwen-vl-ocr-api-reference`
- Qwen3-VL-30B-A3B-Instruct：`https://huggingface.co/Qwen/Qwen3-VL-30B-A3B-Instruct`
- Mistral OCR API：`https://docs.mistral.ai/capabilities/OCR/basic_ocr`
- GOT-OCR2 本地模型：`https://huggingface.co/docs/transformers/model_doc/got_ocr2`
- Surya 本地 OCR：`https://github.com/datalab-to/surya`
- XlsxWriter 超链接写入与可选 `embed_image()`：`https://xlsxwriter.readthedocs.io/worksheet.html`

---

## 15. 外部原图链接规则

最终 Excel 默认不嵌入原始图片文件。`原图` 列必须写入外部原图文件名和相对链接，原图文件放在 Excel 同目录的 `_原图` 文件夹中。

固定执行要求：

- 原图仍使用原始 JPG / PNG 文件，不生成低清二次压缩图。
- 每条主表记录对应一个外部原图文件。
- `原图` 列单元格显示外部原图文件名，并写入相对链接。
- 链接文件名必须可读，至少包含序号、日期、车号、驾驶员和原始文件名。
- `原图` 链接单元格必须随所在行排序、筛选、剪切、复制和移动。
- 必须生成 `原图索引.csv`，便于脱离 Excel 时核对图片来源；索引内必须包含原图相对路径，不写本机绝对路径作为交付依赖。

指定实现方式：

- 创建 `运输录入_<日期范围>_原图` 文件夹。
- 将每行对应原图复制到该文件夹。
- Excel `原图` 列写入 `external:` 相对链接或等效相对文件链接。
- `原图` 列宽度可参考 `28`。
- 数据行高度可参考 `24`。
- 如业务明确要求单文件交付，才允许选择“格内嵌图（文件大）”；该模式可使用 XlsxWriter `worksheet.embed_image()` 或等效 Excel Picture in Cell / richData 结构。

禁止做法：

- 不允许只写普通文本文件名但没有链接。
- 不允许生成低清压缩图替代原图。
- 不允许 Excel 与 `_原图` 文件夹分离交付。
- 不允许为了缩小 Excel 体积把原图压成模糊缩略图。
- 不允许链接数量、索引数量或外部原图文件数量与主表数据行数不一致。

最终检查追加项：

- Excel 是否能快速打开、滚动不卡顿。
- `原图` 列是否为可点击链接。
- 点击链接是否能打开清晰原图。
- 筛选、排序、移动行后，`原图` 链接是否仍对应该行。
- `_原图` 文件夹和 `原图索引.csv` 是否随 Excel 一起保留。

---

## 16. 软件更新规则

- 软件启动后可以自动检查 GitHub Release 最新版本；发现高于当前版本时提示用户更新。
- 自动更新要求当前电脑能访问对应 GitHub Release；如果仓库保持私有，未授权电脑只能提示更新源不可访问，不得硬编码 GitHub 私人访问令牌。
- 顶部工具条必须提供 `检查更新` 手动入口，左侧批次区不得重新堆放入口按钮。
- 下载新版 EXE 时，进度必须显示在中间流程进度条和日志中。
- 更新文件如带 SHA256 摘要，必须校验一致后才允许替换。
- 正在运行的 EXE 不直接覆盖自身；必须先下载到本机临时更新目录，退出主程序后再由更新脚本替换。
- 替换前必须把旧版 EXE 备份到程序旁边的 `更新备份` 文件夹。
- 更新逻辑不得写入或上传 ZHIPU API Key、GPS Authorization、Cookie 等私密配置。
- 源码应按功能逐步拆分模块，至少把自更新逻辑、主界面三栏面板与主业务流程分开，便于后续维护。
