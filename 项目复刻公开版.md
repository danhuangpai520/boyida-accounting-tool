# 保谊达车队做账工具项目复刻公开版

> 本文档用于公开 GitHub 同步，目标是让新的 Codex 对话、其他账号或本地模型拿到仓库后，能按同一目标、边界和验证节奏复刻项目。
> 本文档不得写入真实 API Key、GPS Authorization、Cookie、客户原图、私有批次数据或个人本机敏感路径。

## 1. 项目目标

本项目是 Windows 桌面端运输单据做账工具，把一个批次的磅单、送货单、发货单、调拨单图片转换为可做账、可核对、可追溯原图的 Excel。

固定核心产物：

- `运输录入_<日期范围>.xlsx`
- 同名 `_原图` 文件夹
- OCR 原始 JSON、结构化 JSON/CSV、复核记录、质量报告和错误日志

硬性原则：

- 不能在 OCR 失败、空 OCR、旧 OCR 混入当前批次时继续生成看似完整但不可信的 Excel。
- 不猜测结算字段；无法确认的字段留空或进入备注/复核。
- 规则命中、OCR 原图暂定、无明确路线三种状态必须分开，不得混成正式规则。
- 原图追溯不能丢，Excel 交付必须能回看对应图片。

## 2. 工作边界

开发和复刻默认在仓库工作副本内完成，原始业务项目或历史目录只读参考，不直接改。

本机当前硬边界：

- 原项目 `C:\Users\Administrator\Desktop\zhipu` 只读，只能用于核对历史规范、旧版产物和已知业务事实。
- 实际工作副本是 `C:\Users\Administrator\Desktop\token\zhipu_workcopy`，所有文档复刻、源码索引、验证命令和 GitHub 同步都以这里为准。
- 多线程协作时，模块线程只能修改自己被授权的范围；文档线程只写文档，不能顺手改源码、EXE、`_internal`、批次数据或密钥文件。

推荐目录概念：

```text
<workspace>\
  zhipu_workcopy\              # 当前可修改工作副本
    做账执行工具.exe
    _internal\
    README.md
    做账执行规范_公开版.md
    项目复刻公开版.md
    项目源码索引_公开版.md
    项目资料\
      开发源码\
        zhipu_accounting_app.py
        boyida_workflow.py
        boyida_ui_panels.py
        设置内置Key并打包.ps1
      安装包\
        boyida_setup.iss
        构建安装包.ps1
        uninstall_cleanup.ps1
      说明与发布\
        使用说明.md
```

不要提交或公开：

- `做账执行规范.md`
- `builtin_api_key.txt`
- `gps_api_config.json`
- `config.json`
- `.env`
- 客户原图、批次输出、EXE、`_internal`、安装包输出

协作节奏：

- 大范围改动默认采用 T0 主控 + 模块线程/子任务并行推进。
- T0 只负责边界、证据、冲突合并和最终审计；文档、安装包、扫描监听、OCR/API、UI、Excel/解析、QA/GitHub 应拆给对应模块处理。
- 2026-06-24 起，模块线程不是只读摆设：T2/T3/T4/T8 等 worker 在用户授权的边界内真正执行。T4 的边界是公开/本机文档同步、版本历史、安全扫描证据和 GitHub 白名单建议；不得修改 Python、PowerShell、Inno、EXE、`_internal`、密钥或批次数据。
- 能并行的文件读取、安全扫描、构建检查和烟测应并行执行，再由 T0 合并证据。
- 不得因为开了多个线程就同时写同一个 checkout 的同一片代码；需要合并时由 T0 做最终落地。
- 任一模块落地安装、扫描监听、文档安全、OCR/API、解析/Excel、UI 或发布行为变化后，必须同步公开文档和本机手册；没有同步验证状态的变更不得交给 T0 标记为最终可交付。

### 2.1 公开版可复刻/不可复刻边界

公开文档能复刻：

- 目录结构、公开源码入口、关键函数/类索引和模块职责。
- 公开可追踪资源：`项目资料\开发源码\assets\boyida_truck.png`、`boyida_truck.ico`、`jingzhe_header_line.png`，用于窗口图标和卡车线稿 UI。
- 源码验证、onedir 构建形态、Inno 安装器流程、临时目录安装/卸载 smoke 和当前产物哈希。
- UI/交互合同：三栏布局、标题栏汉堡菜单、左下设置、OCR/API 额度短行、唯一主日志区。
- OCR 控制行为：OCR 引擎和接口/模式主入口已改为按钮弹出选择面板，旧 adapter 只保留兼容数据流。
- 扫描监听：`扫描导入`、批次发现、文件稳定等待、立即/稍后/忽略、成功后 `processed`、失败可重试。
- 安装默认创建当前用户桌面快捷方式，卸载清理快捷方式、安装目录、配置目录和卸载注册表项。

公开文档不能复刻：

- 真实 API Key、Cookie、GPS Authorization、JSESSIONID、账号密码。
- 客户真实批次、客户原图、私有 OCR JSON、私有出表结果和本机完整执行稿。
- 真实 GPS 后端授权；公开版只能复刻配置入口、DPAPI/本机保存边界、无授权时不硬编码凭据的安全行为。
- 本机完整打包用的 `.vendor-sdk` 和 `pyinstaller_hooks` 当前属于本机构建辅助材料，不作为公开文档白名单提交对象；公开克隆若缺失，需要重新安装/生成等价依赖材料，或以本机版本备份/重新打包产物哈希作为交付证据。

公开复刻只使用相对仓库路径和占位配置。需要联网 OCR 或 GPS 时，新电脑必须在本机软件配置、环境变量或私下授权文件中重新填写，不得把凭据补进公开 Markdown 或 Git 历史。

## 3. 源码入口

主入口：

```text
项目资料\开发源码\zhipu_accounting_app.py
```

关键职责：

- UI 主窗口：`AccountingApp`
- 配置目录：`%APPDATA%\ZhipuAccountingTool\config.json`
- 安装/运行根目录：`app_base_dir()`
- 当天批次目录：`managed_workspace_dir()`
- 默认输入目录：`managed_input_dir()`
- 默认输出目录：`managed_output_dir()`
- 一键链路：`run_all()` -> `_run_all_impl()`
- OCR 门禁：`_preflight_ocr_for_generation()`、`ocr_json_status()`
- 解析与出表：`_parse_and_excel_impl()`
- 扫描监听：`scanner_inbox_dir()`、`find_pending_scanner_batch()`、`_poll_scanner_inbox()`

辅助模块：

- `boyida_workflow.py`：工作模式、状态摘要、交付摘要、路线待核判断。
- `boyida_ui_panels.py`：左中右三栏 UI 面板。
- `run_boyida_batch.py`：命令行批处理辅助。

打包资源和构建材料：

- `assets\boyida_truck.png`：窗口/品牌资源。
- `assets\boyida_truck.ico`：EXE 和安装器图标。
- `assets\jingzhe_header_line.png`：当前左栏透明卡车线稿。
- `zhipu_accounting_tool.spec`：onedir PyInstaller 配置，声明图片资源、可选 `.vendor-sdk`、Tcl/Tk 运行库和 `pyinstaller_hooks`。
- `.vendor-sdk`：本机完整工作副本中的云厂商 SDK 依赖缓存；公开克隆可用等价依赖安装替代，但不能把真实 Key 写入其中。
- `pyinstaller_hooks`：本机打包 hook 目录；公开克隆缺失时需按当前 spec/脚本重新补齐等价 hook。

## 4. UI 合同

整体风格是克制的大厂后台工具，不做花哨装饰。

固定结构：

- 左侧模块导航：`批次`、`OCR`、`规则管理`、`复核`、`出表设置`、`GPS 设置`、`检查更新`、底部 `设置`、`帮助`
- 顶部主动作：`导入图片`、`一键出表`、`疑问复核`、`打开结果`
- 中间：批次总览、OCR 到 Excel 工作流程、疑问复核列表、执行日志
- 右侧：GPS 实时、OCR 引擎、动态接口/本地模式、OCR/API 额度
- 标题栏右上角第一个按钮是三条杠菜单；左侧底部设置才是齿轮

OCR 控制约束：

- 不显示 `工作模式`
- 引擎短名：`智谱`、`阿里云`、`百度`、`腾讯云`、`本地 PaddleOCR`
- 引擎下方配置随引擎切换
- OCR 引擎和 OCR 配置不再使用窄小 `ttk.Combobox` 作为主入口；主界面点击按钮弹出选择面板，选择后回填当前值，并保留 adapter 兼容旧代码的数据流
- 下拉/选择面板按屏幕高度自适应，不脱离窗口可见范围
- OCR/API 额度用短行展示，厂商列和额度列对齐，不放手动刷新按钮

2026-06-24 小屏/VM 自适应要点：

- 侧栏、左右栏和顶栏使用紧凑/超紧凑尺寸，避免小屏或 VM 中主按钮、设置入口和右侧 OCR/GPS 面板被裁切。
- 中间 OCR 到 Excel 流程 pipeline canvas 按实际控件高度绘制，不依赖固定大高度，避免节点、按钮和流程文字重叠。
- 右侧 OCR 快速控制只保留引擎和接口/模式两层，选择动作通过弹出面板完成。

## 5. 扫描导入

软件启动时会在安装目录旁创建：

```text
<安装目录>\扫描导入\
```

支持两种批次结构：

```text
扫描导入\2026-06-24\scan001.jpg
扫描导入\2026-06-24\待处理图片\scan001.jpg
```

监听逻辑：

- 软件运行时定时扫描 `扫描导入` 根目录。
- 新图片进入后先等待文件稳定，避免扫描仪还没写完就处理。
- 检测到稳定批次后弹窗，显示图片数量和批次路径。
- 按钮固定为 `立即一键出表`、`稍后提醒`、`忽略本批`。
- 点击立即后只设置当前图片目录、输出目录和 OCR JSON 路径，然后调用现有 `run_all()`。
- 不绕过 OCR 预检、空 OCR 硬停止、复核和 Excel 规则。
- 已处理、忽略、稍后提醒状态写入本机配置，避免同一批反复弹窗。
- 软件未运行期间进入的批次，下次启动后会补扫。

## 6. OCR/API 安全边界

公开仓库只保存接口目录、调用规则和配置占位。

Key 来源优先级：

- 环境变量
- 本机安全配置
- 用户在软件内导入或填写
- 私下内部包可能存在的内置默认 Key

公开版 EXE 和公开文档不得内置或展示真实 Key。

千问视觉 OCR（百炼 OpenAI 兼容接口）作为独立在线 OCR 引擎登记，默认 / 优先接入模型为官方 OCR 专用 `qwen-vl-ocr`，同时可选 `qwen3-vl-plus` 和兼容旧 `qwen3.7-plus`，模型名必须可配置。公开复刻只记录安全边界：密钥只能来自环境变量、本机安全配置或本机导入文件；允许登记的环境变量名是 `DASHSCOPE_API_KEY`、`BAILIAN_API_KEY`、`QWEN_API_KEY`，允许记录的本机导入文件名是 `qwen.txt` / `qwen.csv` 等，不得写入任何实际值。

当前源码已包含千问视觉 OCR 的引擎入口、模型接口目录、环境变量/本机配置读取、OpenAI 兼容请求适配和中间层输出。公开文档仍不得把真实密钥、客户图片或私有批次写入仓库；真实百炼网络调用状态以部署机器的 Key、模型开通和样本验证为准。

复刻时可运行 `python 项目资料\开发源码\zhipu_accounting_app.py --qwen-key-template` 在本机私有 `API KEY` 文件夹生成 `qwen.example.txt`，再由使用者在本机复制为 `qwen.txt` 并填写，或改用 `qwen.csv` 保存字段名对应的本机值。`python 项目资料\开发源码\zhipu_accounting_app.py --qwen-preflight` 用于无联网预检本机是否已有凭据、模型和 endpoint 候选；`python 项目资料\开发源码\zhipu_accounting_app.py --qwen-live-probe` 只用于本机真实调用探针；无 Key 返回 `QWEN_LIVE_PROBE_MISSING_KEY`，有 Key 且模型开通才可返回 `QWEN_LIVE_PROBE_OK`，不得把 Key 写进命令行、Markdown、源码、CSV 样例或 Git。

OCR 失败处理：

- API Key 错误、网络错误、额度不足、文本为空、图片覆盖不完整，都必须停止。
- 不允许生成空白或半可信 Excel。
- 已有 OCR JSON 只有在覆盖当前全部图片、无 error、无空文本、无旧图片混入时才可复用。
- 千问视觉 OCR 输出也不能直接写最终做账字段；必须继续经过解析、疑问复核、Excel 生成和原图追溯，遵守空 OCR 硬停止和不猜字段规则。

## 7. Excel 与规则

主表固定字段：

```text
日期、车号、驾驶员、重量、货物名称、装货地、收货地、备注、原图
```

规则执行顺序：

1. OCR 识别图片文本。
2. 解析单据字段。
3. 应用内置做账路线规则。
4. 应用人工纠错/路线覆盖规则。
5. 生成待复核记录和质量报告。
6. 生成 Excel 和 `_原图` 文件夹。

重量只取 `净重` 或 `结算重量`。`毛重`、`皮重`、`空重` 不相减推导最终重量。

## 8. 打包与安装

稳定运行源仍是 PyInstaller onedir：

```text
做账执行工具.exe
_internal\
```

安装包使用 Inno Setup：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\项目资料\安装包\构建安装包.ps1
```

默认安装位置：

```text
%LocalAppData%\Programs\BoyidaAccountingTool
```

安装器职责：

- 复制 `做账执行工具.exe` 和 `_internal`
- 创建开始菜单/默认当前用户桌面快捷方式
- 创建 `扫描导入` 文件夹
- 随安装复制公开说明文档
- 标准卸载程序文件和快捷方式

2026-06-24 安装器工作副本状态：

- `项目资料\安装包\boyida_setup.iss` 已删除可选 desktop task。
- 安装器默认创建 `{userdesktop}\保谊达车队做账工具`，目标为 `{app}\做账执行工具.exe`。
- 当前 `做账执行工具.exe` SHA256：`9515D9709BF46742C734620E5859FEB7B06D06F2D6CA883DA18FBCFD27CC2AFD`。
- 当前安装器 `项目资料\安装包\输出\BoyidaAccountingTool_Setup_v2.3.exe` SHA256：`2DE43A349C9645552915FED807112A05A8E2BC9F55E9EBDB6C951CBF5DE32292`。
- 包体大小本轮按用户要求暂不处理。

卸载清理：

- 完整清理时删除安装目录下 `扫描导入`、程序生成的当天批次目录和 `%APPDATA%\ZhipuAccountingTool`
- 用户选择保留业务文件时，只卸载程序文件
- 删除脚本必须做路径边界检查，不得越界删除

## 9. 验证命令

在源码目录执行：

```powershell
python -m py_compile .\项目资料\开发源码\zhipu_accounting_app.py
python .\项目资料\开发源码\zhipu_accounting_app.py --self-test
python .\项目资料\开发源码\zhipu_accounting_app.py --startup-smoke-test
python .\项目资料\开发源码\zhipu_accounting_app.py --tray-menu-probe
python .\项目资料\开发源码\zhipu_accounting_app.py --scanner-watch-probe
```

安装包验证建议：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\项目资料\安装包\构建安装包.ps1 -SkipAppBuild
```

再用临时安装目录做安装、EXE 自检、启动冒烟和卸载清理检查。

2026-06-24 已验证状态：

- Python 3.11 `py_compile`、`--self-test`、`--startup-smoke-test`、`--tray-menu-probe`、`--scanner-watch-probe` 已通过。
- 安装器本机临时目录静默安装/卸载 smoke 已通过，确认桌面快捷方式创建、快捷方式目标正确，卸载后快捷方式、安装目录、配置目录和卸载注册表清理干净。
- 尚未在用户真实 VM 中亲手运行；真实 VM 结果不得用本机 smoke 替代。

## 10. GitHub 文档同步

当前自动同步纪律是：后续变更只用白名单同步公开安全文档和公开索引，不用 `git add .`。历史仓库里已经存在的源码或发布文件不代表以后每次都继续同步软件本体。

允许同步：

- `README.md`
- `做账执行规范_公开版.md`
- `项目复刻公开版.md`
- `项目源码索引_公开版.md`
- `版本历史.md`
- `项目资料\说明与发布\使用说明.md`

提交前必须检查：

- staged 文件中没有 `.exe`
- 没有 `_internal`
- 没有私有规范、Key、Cookie、GPS 授权
- 没有客户原图或批次输出

推荐同步方式：

```powershell
# 本机工作副本存在同步脚本时可用：
Set-Location C:\Users\Administrator\Desktop\token\zhipu_workcopy
powershell -NoProfile -ExecutionPolicy Bypass -File .\项目资料\开发源码\同步公开文档到GitHub.ps1 -Push
```

如果公开克隆中没有本机同步脚本，就手动执行等价白名单提交，只添加上面列出的公开文档，不使用 `git add .`。

每次提交前还要做三步检查：

1. `git status --short` 确认暂存区只包含白名单公开文档。
2. 运行本文档下方敏感扫描命令，确认无疑似真实凭据。
3. 在提交说明或 `版本历史.md` 里写清楚哪些验证已通过、哪些仍未验证；不得把“已写文档”说成“真实业务链路已跑通”。

## 11. 冷启动复刻核对表

站在“另一个账号/新电脑只看公开 GitHub 文档”的角度，按下表逐项核对；未通过的项目不能写成已验证。

| 步骤 | 所需文件 | 验证命令/动作 | 通过标志 | 公开边界 |
|---|---|---|---|---|
| 1. 克隆并定位 | 公开仓库 | 读 `README.md`、`做账执行规范_公开版.md`、本文档、`项目源码索引_公开版.md`、`版本历史.md` | 能说明产品目标、公开/私有边界、当前哈希和验证命令 | 不依赖私有手册 |
| 2. 核对目录结构 | `项目资料\开发源码`、`项目资料\安装包` | `Test-Path .\项目资料\开发源码\zhipu_accounting_app.py`；`Test-Path .\项目资料\安装包\boyida_setup.iss` | 两项均为 `True` | 不需要本机绝对路径 |
| 2.1 核对打包资源 | `assets`、`zhipu_accounting_tool.spec` | `Test-Path .\项目资料\开发源码\assets\jingzhe_header_line.png`；`Test-Path .\项目资料\开发源码\zhipu_accounting_tool.spec` | 两项均为 `True`；资源哈希可对照源码索引 | `.vendor-sdk`/hooks 若缺失需重建等价构建材料 |
| 3. 源码基础验证 | `zhipu_accounting_app.py` | `python -m py_compile .\项目资料\开发源码\zhipu_accounting_app.py`；`python .\项目资料\开发源码\zhipu_accounting_app.py --self-test` | 退出码 0 | 不代表真实 OCR Key 可用 |
| 4. UI/交互验证 | 主程序源码 | `python .\项目资料\开发源码\zhipu_accounting_app.py --startup-smoke-test`；`python .\项目资料\开发源码\zhipu_accounting_app.py --tray-menu-probe` | `STARTUP_SMOKE_OK`、`TRAY_MENU_PROBE_OK` | 只验证入口接线和安全交互，不发送真实任务 |
| 5. 扫描监听验证 | 主程序源码 | `python .\项目资料\开发源码\zhipu_accounting_app.py --scanner-watch-probe` | `SCANNER_WATCH_PROBE_OK` | 使用临时测试图片/目录，不使用客户原图 |
| 6. 安装器验证 | `做账执行工具.exe`、`_internal`、安装脚本 | `powershell -NoProfile -ExecutionPolicy Bypass -File .\项目资料\安装包\构建安装包.ps1 -SkipAppBuild`，再临时目录静默安装/卸载 | 桌面快捷方式存在且指向安装目录 EXE；卸载后快捷方式、安装目录、配置目录、卸载注册表清理干净 | 不扩大卸载删除范围 |
| 7. 产物哈希 | EXE、安装器输出 | `Get-FileHash .\做账执行工具.exe -Algorithm SHA256`；`Get-FileHash .\项目资料\安装包\输出\BoyidaAccountingTool_Setup_v2.3.exe -Algorithm SHA256` | EXE 为 `9515D9709BF46742C734620E5859FEB7B06D06F2D6CA883DA18FBCFD27CC2AFD`；安装器为 `2DE43A349C9645552915FED807112A05A8E2BC9F55E9EBDB6C951CBF5DE32292` | 只证明公开产物一致 |
| 8. 凭据与客户数据 | 公开 Markdown 和 Git 暂存区 | 运行下方敏感扫描命令；提交前只白名单添加公开文档 | 无疑似真实凭据；没有客户原图/批次输出进入暂存 | 只能复刻配置入口和安全行为 |
| 9. 在线能力复刻 | 本机凭据配置入口 | 在软件内填写或导入本机 OCR/GPS 凭据后单独验证 | 有授权时在线 OCR/GPS 正常；无授权时明确失败且不生成可信 Excel | 公开文档不提供真实授权 |

敏感扫描命令：

```powershell
$secretPattern = 'AKIA[0-9A-Z]{16}|sk-' + '[A-Za-z0-9_-]{20,}|JSESSIONID' + '=[^;\s]+|Authorization' + ':\s*Bearer\s+\S+|Cookie' + ':\s*\S+='
Select-String -Path .\README.md,.\做账执行规范_公开版.md,.\项目复刻公开版.md,.\项目源码索引_公开版.md,.\版本历史.md -Pattern $secretPattern
```

## 12. 变更纪律

任何后续修改都要同步更新：

- 公开复刻文档
- 本机完整复刻手册
- 使用说明
- 相关规范
- 测试/验证结果

不能把“计划完成”说成“验证完成”。安装包、扫描监听、GitHub 推送都要分别给出真实验证状态。

功能变更同步门禁：

- UI/交互变化：更新 `README.md`、本文档、`项目源码索引_公开版.md`、`项目资料\说明与发布\使用说明.md` 和本机完整复刻手册。
- OCR/API/密钥变化：更新 `做账执行规范_公开版.md`、本文档、使用说明和版本历史；公开文档只写变量名、入口和失败状态，不写真实值。
- Excel/解析/规则变化：更新执行规范、源码索引、复刻文档和版本历史；必须说明字段、页签、原图追溯和待复核状态是否变化。
- 打包/安装/发布/GitHub 变化：更新 README、复刻文档、源码索引、使用说明和版本历史；必须列明 EXE/安装器哈希、烟测范围和未验证点。
